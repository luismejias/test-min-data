
import { DashboardPageComponent } from './dashboard-page.component';

describe('DashboardPageComponent', () => {
  let component: DashboardPageComponent;

  beforeEach(() => {
    component = new DashboardPageComponent();
  });
  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
