import { DashboardTableComponent } from './dashboard-table.component';

describe('DashboardTableComponent', () => {
  let component: DashboardTableComponent;

  beforeEach(() => {
    component = new DashboardTableComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
